# Runtime config for build/config.js
OS_CLIENT_ROOT=http://localhost:3002/app
OS_API_ROOT=https://soporte.cod3onestudios.com/api
OS_GLOBAL_INDEX_PATH=/app
OS_SHOW_LOGS=true

# Dev server config
OS_DEV_SERVER_HOST=0.0.0.0
OS_DEV_SERVER_PORT=3002

# Optional: keep OS_API_ROOT=/api and proxy requests to a remote backend to avoid CORS
# OS_DEV_PROXY_TARGET=https://remote.example.com/api